// app/admin/layout.jsx

// Ce layout de base s'assure juste que le contenu de la page admin est rendu.
// On pourrait ajouter ici un contexte spécifique à l'admin si besoin plus tard.
export default function AdminRootLayout({ children }) {
  return <>{children}</>;
}
